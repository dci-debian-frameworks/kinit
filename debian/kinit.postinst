#!/bin/sh
# kinit.postinst, runs ldconfig
set -e

case "$1" in
       configure)
                ;;
        abort-upgrade|abort-remove|abort-deconfigure)
                ;;
 esac

ldconfig

#DEBHELPER#
################################################## 

exit 0